---
description: Gene 控制器与请求/视图用法约定
globs: "**/*.php"
alwaysApply: false
---

# Gene 控制器与请求/视图

- 控制器继承 `\Gene\Controller`，通过 `$this->db`、`$this->cache`、`$this->validate` 等使用注入组件。
- 获取请求：用 `Controller::get($key, $default)` / `post()` / `params($key)`，不要直接读 `$_GET`/`$_POST`。
- 视图：用 `$this->assign($name, $value)` 赋值，用 `$this->display($file, $parent_file)` 渲染；多语言链接用 `Controller::url($path)`。
- 统一输出：AJAX/API 用 `Controller::success()` / `error()` / `data()` / `json()`，保持响应格式一致。
