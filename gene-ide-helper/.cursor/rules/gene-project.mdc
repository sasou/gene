---
description: 本仓库 Gene 项目约定：控制器/路由/配置/校验/响应/Service/Model
globs: "**/*.php"
alwaysApply: false
---

# Gene 项目约定（本仓库）

## 控制器与命名空间

- 控制器一律继承 `\Gene\Controller`，命名空间与目录一致：
  - `Controllers\` → `application/Controllers/`
  - `Controllers\Gateway\`、`Controllers\Admin\` 等同理
- 取参：`$this->request->post`、`$this->request->get()`、`$this->request()`（合并）
- 校验：`$this->validate->init($data)->name('field')->required()->msg('错误')->valid()`，失败时 `return $this->error($this->validate->error())`
- 统一响应：`return $this->success('消息', 数据)`、`return $this->error('消息')`、`return $this->data($data)`；钩子或需裸 JSON 时用 `\Gene\Response::json(\Gene\Response::error('...'))`
- 控制器主要功能是取参、校验、响应

## 视图

- 传变量：`$this->assign('name', $value)`
- 渲染：`$this->display('子目录/视图名', '子目录/layout')`，例如 `web/index`+`web/layout`

## 路由（config/router.ini.php）

- `$router->get/post(path, "Controllers\Xxx@action", "hook@clearAfter")`；第三项为钩子与输出处理，如 `@clearAfter`、`adminAuth@`、`webAuth@clearAfter`
- 分组：`->group("/admin/")` 内用 `Controllers\Admin\:c@:a`，`->group("/im/")` 内用 `Controllers\Im\:c@:a`
- 钩子用 `->hook("adminAuth", function() { ... })` 中设置 `\Gene\Di::set('user', $user)`，未登录时 `$this->response->redirect(...)` 或 `\Gene\Response::json(\Gene\Response::error(...)); return false;`

## 配置（config/config.ini.{env}.php）

- `$config->set("key", ['class' => '\Gene\Xxx', 'params' => [[...]], 'instance' => true])`；控制器内通过 `$this->key` 访问注入组件（如 `$this->db`、`$this->cache`、`$this->validate`、`$this->websession`、`$this->memcache`、`$this->redis`、`$this->language`、`$this->adminsession`）

## Service 与 Model

- Service 继承 `\Gene\Service`，单例 `Xxx::getInstance()`，内部可 `return $this->error()/success()/data()`，调用 Model 用 `\Models\Im\Xxx::getInstance()->row($id)` 等,封装业务逻辑及缓存
- Model 继承 `\Gene\Model`，用 `$this->db->select/count("表名", "字段")->where(...)->order(...)->limit(...)->row()/all()/cell()`，封装数据调用

## 入口

- Web：`Application::getInstance()->autoload(APP_ROOT)->load("router.ini.php", CONF_DIR)->load("config.ini.{$env}.php", CONF_DIR)->setMode(1, $debug)->run()`
- CLI：`->run('get', $path)`，path 来自 `$_SERVER['argv'][1]`
